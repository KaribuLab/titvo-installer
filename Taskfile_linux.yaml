version: '3'

tasks:
  build:
    desc: Compila todos los comandos en el directorio cmd
    cmds:
      - mkdir -p bin
      - go build -o ./bin/titvo ./cmd/titvo-installer
    sources:
      - ./cmd/**/*.go
      - ./internal/**/*.go
      - go.mod
      - go.sum